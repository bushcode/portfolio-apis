const {
  CreateProject,
  FetchProject,
  FetchProjects,
  DeleteProject,
} = require('../controllers/projects.controller');

test('should create a new project in the database', () => {});
test('should return an object of project in the database', () => {});
test('should return all projects in the database', () => {});
test('should delete a project from the database', () => {});
